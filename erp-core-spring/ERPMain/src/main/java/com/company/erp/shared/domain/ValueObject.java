package com.company.erp.shared.domain;

/**
 * Marker interface for value objects.
 * Value objects are immutable, have no identity, and are identified by their values.
 *
 * Examples: Money, PhoneNumber, Email, Address
 * Implements equals/hashCode based on all fields (not identity).
 */
public interface ValueObject {

}

